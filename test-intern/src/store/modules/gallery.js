const state = {
  // isLoading: false,
};

const mutations = {
  isLoadingStart(state) {
    // isLoading = true;
  },
  isLoadingFinish(state) {
    // isLoading = false;
  },
};

const actions = {

};

export default {
  state,
  mutations,
};
